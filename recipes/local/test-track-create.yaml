version: 1.0.0
# type: coordinator
title: "Test Harness — Track Create"
description: "Test harness for the refactored track-create primitive. Points at EduPlay project."

parameters: []

sub_recipes:
  - name: "track_create"
    path: "../agents/conductor/track-create.yaml"
    description: "Author track artifacts."

instructions: |
  You are a test harness. Your ONLY job is to delegate to the
  track_create sub-recipe using the subagent() tool call mechanism
  with the parameters specified in the prompt.

  Do NOT read the sub-recipe file. Do NOT do the work yourself.
  Do NOT explore any codebase. ONLY invoke the sub-recipe via the
  subagent tool and report what comes back.

prompt: |
  Call the track_create sub-recipe with these exact parameters:

  subagent(subrecipe: "track_create", parameters: {
    project_path: "C:/Users/donid/ClaudeProjects/Eduplay",
    project_kind: "sandbox",
    project_json_path: "C:/Users/donid/ClaudeProjects/Eduplay/conductor/project.json",
    project_id: "eduplay",
    track_type: "feature",
    shortname: "test-hygiene-check",
    title: "Test Hygiene Check",
    requirements_brief: "Add a simple health check endpoint at /api/health that returns JSON with status ok and the current server timestamp. This is a test of the conductor track-create primitive.",
    phases_json: "",
    dependencies_json: "",
    live_confirmed: false
  })

  After the sub-recipe returns, show me the FULL return value
  (every field, formatted as a code block). Then state whether
  the test PASSED (status is ok or success) or FAILED (any other status).
